// TODO: Move to one single source of truth, where App.tsx is building the routes from
export enum Pages {
    HOME_PRIVAT = "/home",
    MESSAGES_PRIVAT = "/messages",
    MANAGE_USERS_PRIVAT = "/users",
    LOGIN_PUBLIC = "/login",
    ACKNOWLEDGEMENTS_PUBLIC = "/acknowledgements",
    GAME_HISTORY_PUBLIC = "/game-history",
}
